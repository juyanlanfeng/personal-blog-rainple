# 图片保护说明

已移除密码登录，改为「公开可看、难以保存」三层防护。

## 一、构建时水印（scripts/watermark.js）

`npm run build` 的最后一步自动给 **dist/oc/art、dist/oc/fursuit** 下所有图片加平铺半透明水印（"雨枫 rainple"，斜向重复）。**public/ 里的原图完全不动**，只有构建产物带水印，线上永远不出现无水印图。gif 和视频跳过。

可调参数在脚本顶部：`TEXT`（文字）、`OPACITY`（透明度）、`ANGLE`（角度）、`FONT`（字体）。

首次构建后请抽查一张 `dist/oc/art/` 里的图确认效果；若中文显示为方块，把 `TEXT` 改成纯英文。

注意：图片较多（约 7GB），每次 build 会多花一些时间处理水印。

## 二、禁右键 / 禁拖拽 / 透明遮罩

三处页面均已加上：博客全站（`src/layouts/Layout.astro`）、OC 主页（`src/pages/oc/_page.html`）、相册整页（`src/pages/oc/_gallery-page.astro`）。

- 图片上右键不出「图片另存为」菜单，无法拖拽保存
- 相册卡片和灯箱大图对鼠标"隐形"（等效透明遮罩），右键落在容器上
- 点击看大图等交互不受影响

## 三、如何测试

本地跑 `npm run dev` 就能测试禁右键/禁拖拽；水印只在 `npm run build` 后的 dist 里，本地开发看到的是原图，属正常。

## 诚实的边界

这套方案挡的是"随手另存"的普通访客。截图、开发者工具、直接请求图片 URL 依然可行——任何公开网站都无法杜绝。水印是兜底：即使被拿走，图上也带着你的署名。如果某些图绝对不能外流，唯一可靠的办法是不上线，或恢复密码门禁（随时可以再加回来）。
